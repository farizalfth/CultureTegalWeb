tags:
  - System
summary: Periksa status aktif API
description: Mengembalikan status operasional dasar dari server API Tegal Culture.
responses:
  200:
    description: API aktif dan berjalan normal
    schema:
      type: object
      properties:
        status:
          type: string
          example: success
        message:
          type: string
          example: API v1 is active.